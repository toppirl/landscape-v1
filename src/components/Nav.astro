---
import navData from '../data/navData'
---

<ul class='main-nav'>
  {
    navData.map((item) => (
      <a href={item.path} class='main-nav-links'>
        {item.name}
      </a>
    ))
  }
</ul>
<a href='#' class='btn nav--cta'>Call us</a>

<style>

</style>
